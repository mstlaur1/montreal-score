# Admin API authentication token (required for ETL and promise management APIs)
ADMIN_API_TOKEN=your-secret-token

# Project directory (required for ETL route to run scripts)
PROJECT_DIR=/path/to/montreal-score

# Montreal Open Data (no auth required)
MONTREAL_DATA_API=https://data.montreal.ca/api/3/action
